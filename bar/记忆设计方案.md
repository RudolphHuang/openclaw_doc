# 记忆系统设计方案（Claude Code 文本索引方案）

## 1. 设计概述

### 1.1 核心理念

**记忆是文本，不是向量。判断相关性的是 AI，不是算法。**

本方案采用 Claude Code 的文本记忆架构，以人类可读的 Markdown 文本存储记忆，由 LLM 自主判断记忆的相关性，而非依赖向量相似度计算。

### 1.2 与角色系统的关系

> **当前阶段**：角色系统暂不开发，但保留完整的向后扩展性。
> 
> **agent_id 处理**：所有记忆统一使用固定值 `default_agent`，未来角色系统开发后可无缝迁移。

```mermaid
flowchart TB
    subgraph Future["未来扩展（预留）"]
        Agent1["角色 A"]
        Agent2["角色 B"]
        Agent3["角色 C"]
    end
    
    subgraph Current["当前实现"]
        DefaultAgent["default_agent<br/>(固定值)"]
    end
    
    subgraph MemorySystem["记忆系统"]
        Index["记忆索引"]
        Entries["记忆条目"]
    end
    
    DefaultAgent --> Index
    Agent1 -.->|预留| Index
    Agent2 -.->|预留| Index
    Agent3 -.->|预留| Index
    
    Index --> Entries
```

---

## 2. 系统架构

### 2.1 整体架构

```mermaid
flowchart TB
    subgraph Frontend["前端层 (Next.js + React)"]
        UI["UI 组件层"]
        State["状态管理 (Zustand)"]
        Hooks["自定义 Hooks"]
        MemoryPage["记忆管理页面"]
        ChatToggle["聊天记忆开关"]
    end
    
    subgraph API["API 层 (tRPC / REST)"]
        MemoryAPI["记忆管理 API"]
        ChatAPI["对话 API"]
        ToolAPI["工具调用 API"]
    end
    
    subgraph Service["服务层"]
        MemoryService["记忆服务"]
        ContextService["上下文服务"]
        ToolService["记忆工具服务"]
    end
    
    subgraph Database["数据层 (PostgreSQL)"]
        IndexDB[("text_memory_index<br/>记忆索引表")]
        EntryDB[("text_memories<br/>记忆条目表")]
        SessionDB[("sessions<br/>会话表")]
        MessageDB[("messages<br/>消息表")]
    end
    
    subgraph External["外部服务"]
        LLM["LLM API"]
    end
    
    UI --> State
    State --> Hooks
    Hooks --> API
    MemoryPage --> MemoryAPI
    ChatToggle --> API
    
    MemoryAPI --> MemoryService
    ChatAPI --> ContextService
    ToolAPI --> ToolService
    
    MemoryService --> IndexDB
    MemoryService --> EntryDB
    ContextService --> IndexDB
    ContextService --> EntryDB
    ContextService --> SessionDB
    ContextService --> MessageDB
    
    ContextService --> LLM
    ToolService --> LLM
```

### 2.2 核心数据流

```mermaid
flowchart LR
    subgraph UserLayer["用户层"]
        User["用户"]
    end
    
    subgraph ChatLayer["对话层"]
        Chat["聊天界面"]
        Toggle["记忆开关"]
    end
    
    subgraph ProcessingLayer["处理层"]
        IndexLoad["加载记忆索引"]
        LLMJudge["LLM 判断相关性"]
        ToolCall["工具调用加载详情"]
    end
    
    subgraph StorageLayer["存储层"]
        Index["记忆索引"]
        Entries["记忆条目"]
    end
    
    User --> Chat
    Chat --> Toggle
    Toggle --> IndexLoad
    IndexLoad --> Index
    Index --> LLMJudge
    LLMJudge -->|需要详情| ToolCall
    ToolCall --> Entries
    Entries --> Chat
```

---

## 3. 数据库 Schema 设计

### 3.1 记忆索引表 (text_memory_index)

存储轻量级的记忆摘要列表，始终加载到上下文中。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | varchar(255) | PK | 索引唯一标识 |
| user_id | text | FK → users.id | 所属用户 |
| agent_id | varchar(100) | Default 'default_agent' | 所属角色（预留） |
| category | varchar(50) | - | 记忆分类 |
| title | varchar(255) | - | 记忆标题 |
| description | text | - | 记忆描述（一行） |
| entry_id | varchar(255) | FK → text_memories.id | 关联条目 ID |
| is_active | boolean | Default true | 是否激活 |
| last_accessed_at | timestamptz | - | 最后访问时间 |
| access_count | bigint | Default 0 | 访问次数 |
| created_at | timestamptz | - | 创建时间 |
| updated_at | timestamptz | - | 更新时间 |

**索引设计**：
```sql
-- 按用户和角色查询索引
CREATE INDEX idx_memory_index_user_agent ON text_memory_index(user_id, agent_id);
-- 按分类筛选
CREATE INDEX idx_memory_index_category ON text_memory_index(category);
-- 按激活状态筛选
CREATE INDEX idx_memory_index_active ON text_memory_index(is_active);
```

### 3.2 记忆条目表 (text_memories)

存储完整的 Markdown 记忆内容。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | varchar(255) | PK | 条目唯一标识 |
| user_id | text | FK → users.id | 所属用户 |
| agent_id | varchar(100) | Default 'default_agent' | 所属角色（预留） |
| category | varchar(50) | - | 记忆分类 |
| title | varchar(255) | - | 记忆标题 |
| content | text | - | 记忆内容（Markdown） |
| source_session_id | text | FK → sessions.id | 来源会话 ID（可选） |
| source_message_ids | jsonb | - | 来源消息 ID 列表 |
| is_active | boolean | Default true | 是否激活 |
| created_at | timestamptz | - | 创建时间 |
| updated_at | timestamptz | - | 更新时间 |

**索引设计**：
```sql
-- 按用户和角色查询条目
CREATE INDEX idx_memories_user_agent ON text_memories(user_id, agent_id);
-- 按分类筛选
CREATE INDEX idx_memories_category ON text_memories(category);
```

### 3.3 记忆分类定义

| 分类 | 说明 | 示例 |
|------|------|------|
| `user` | 用户画像/偏好 | "用户喜欢简洁的回答" |
| `feedback` | 行为指导/反馈 | "用户不喜欢使用表情符号" |
| `project` | 进行中的工作上下文 | "正在开发用户认证模块" |
| `reference` | 外部资源指针 | "参考文档: https://..." |
| `general` | 未分类通用记忆 | "其他重要信息" |

### 3.4 ER 关系图

```mermaid
erDiagram
    text_memory_index ||--|| text_memories : references
    users ||--o{ text_memory_index : owns
    users ||--o{ text_memories : owns
    sessions ||--o{ text_memories : sources
    
    text_memory_index {
        varchar id PK
        text user_id FK
        varchar agent_id "default: default_agent"
        varchar category
        varchar title
        text description
        varchar entry_id FK
        boolean is_active
        timestamptz last_accessed_at
        bigint access_count
        timestamptz created_at
        timestamptz updated_at
    }
    
    text_memories {
        varchar id PK
        text user_id FK
        varchar agent_id "default: default_agent"
        varchar category
        varchar title
        text content "Markdown"
        text source_session_id FK
        jsonb source_message_ids
        boolean is_active
        timestamptz created_at
        timestamptz updated_at
    }
```

---

## 4. 核心功能流程

### 4.1 记忆生命周期

```mermaid
flowchart TB
    subgraph Create["创建记忆"]
        C1["LLM 识别重要信息"] --> C2["调用 memory_create 工具"]
        C2 --> C3["写入 text_memories"]
        C3 --> C4["自动生成索引记录"]
    end
    
    subgraph Read["读取记忆"]
        R1["加载索引到上下文"] --> R2["LLM 判断相关性"]
        R2 -->|需要详情| R3["调用 memory_read 工具"]
        R3 --> R4["查询 text_memories"]
        R4 --> R5["更新访问统计"]
    end
    
    subgraph Update["更新记忆"]
        U1["调用 memory_update 工具"] --> U2["更新 text_memories"]
        U2 --> U3["同步更新索引"]
    end
    
    subgraph Delete["删除记忆"]
        D1["调用 memory_delete 工具"] --> D2["删除 text_memories"]
        D2 --> D3["删除索引记录"]
    end
```

### 4.2 对话中的记忆流程

```mermaid
sequenceDiagram
    autonumber
    actor User as 用户
    participant UI as 前端 UI
    participant API as API 层
    participant CS as 上下文服务
    participant LLM as LLM 服务
    participant DB as 数据库

    User->>UI: 发送消息
    UI->>API: POST /api/chat/message
    API->>DB: 保存用户消息
    
    API->>CS: 构建上下文
    CS->>DB: 查询记忆索引（用户 + default_agent）
    DB-->>CS: 返回索引列表
    
    CS->>LLM: 发送请求（携带记忆索引）
    Note over CS,LLM: 系统提示词包含：<br/>## 可用记忆<br/>1. 用户偏好 - 用户喜欢简洁回答<br/>2. 项目背景 - 正在开发认证模块<br/>...
    
    alt LLM 判断需要记忆详情
        LLM->>API: 调用 memory_read(entry_id)
        API->>DB: 查询记忆条目
        DB-->>API: 返回完整内容
        API-->>LLM: 返回记忆详情
    end
    
    alt LLM 识别新记忆
        LLM->>API: 调用 memory_create(title, content, category)
        API->>DB: 写入记忆条目
        API->>DB: 创建索引记录
    end
    
    alt LLM 更新现有记忆
        LLM->>API: 调用 memory_update(entry_id, content)
        API->>DB: 更新记忆条目
        API->>DB: 更新索引
    end
    
    LLM-->>API: 返回回复
    API->>DB: 保存 AI 消息
    API-->>UI: 返回消息
    UI-->>User: 显示回复
```

### 4.3 索引重建流程

```mermaid
flowchart TD
    Trigger["触发条件"] -->|创建/更新/删除| Rebuild["重建索引"]
    
    Rebuild --> Query["查询 text_memories"]
    Query --> Extract["提取标题和描述"]
    Extract --> Generate["生成索引记录"]
    Generate --> Update["更新 text_memory_index"]
    
    Trigger -.->|定时任务| Cleanup["清理过期索引"]
    Cleanup --> Remove["删除无效关联"]
```

---

## 5. LLM 记忆工具设计

### 5.1 工具列表

| 工具名 | 功能 | 调用时机 |
|--------|------|----------|
| `memory_create` | 创建新记忆 | LLM 识别到值得记住的信息 |
| `memory_read` | 读取记忆详情 | LLM 判断需要某条记忆的完整内容 |
| `memory_update` | 更新记忆 | LLM 发现已有记忆需要修正或补充 |
| `memory_delete` | 删除记忆 | LLM 判断某条记忆已过时或错误 |
| `memory_list` | 列出所有记忆 | LLM 需要了解可用记忆范围 |

### 5.2 工具调用流程

```mermaid
flowchart LR
    subgraph Tools["记忆工具"]
        Create["memory_create"]
        Read["memory_read"]
        Update["memory_update"]
        Delete["memory_delete"]
        List["memory_list"]
    end
    
    subgraph Permissions["权限控制"]
        ReadEnabled["读取开关"]
        WriteEnabled["写入开关"]
    end
    
    subgraph Storage["存储层"]
        Index["索引表"]
        Entry["条目表"]
    end
    
    ReadEnabled --> Read
    ReadEnabled --> List
    WriteEnabled --> Create
    WriteEnabled --> Update
    WriteEnabled --> Delete
    
    Create --> Entry
    Create --> Index
    Read --> Entry
    Update --> Entry
    Update --> Index
    Delete --> Entry
    Delete --> Index
    List --> Index
```

---

## 6. 上下文注入策略

### 6.1 提示词构建

```mermaid
flowchart TB
    Start(["构建提示词"]) --> Base["基础系统提示词"]
    
    Base --> MemorySection["## 记忆系统"]
    MemorySection --> IndexList["可用记忆索引：<br/>1. [user] 用户偏好 - 用户喜欢简洁回答<br/>2. [project] 当前项目 - 正在开发认证模块<br/>3. [feedback] 行为反馈 - 避免使用技术术语"]
    
    IndexList --> Instruction["你可以使用以下工具管理记忆：<br/>- memory_read(entry_id) - 加载记忆详情<br/>- memory_create(...) - 创建新记忆<br/>- memory_update(...) - 更新记忆<br/>- memory_delete(entry_id) - 删除记忆"]
    
    Instruction --> History["对话历史"]
    History --> UserMsg["用户消息"]
    UserMsg --> End(["完整提示词"])
```

### 6.2 Token 控制策略

| 项目 | 估算 Token | 控制策略 |
|------|-----------|----------|
| 基础系统提示词 | 200-500 | 固定 |
| 记忆索引（200条） | ~5000 | 限制每用户最多 200 条 |
| 选中记忆详情 | 可变 | LLM 自主选择加载 |
| 对话历史 | 可变 | 按消息数量限制 |

**溢出处理**：
- 当记忆数量超过 200 条时，LLM 需要合并或删除低价值条目
- 管理页面显示警告提示用户清理记忆

---

## 7. 用户界面设计

### 7.1 聊天界面记忆开关

```mermaid
flowchart TB
    subgraph ChatInput["聊天输入区"]
        Input["输入框"]
        Actions["操作栏"]
    end
    
    subgraph MemoryControls["记忆控制"]
        ReadToggle["📖 读取记忆"]
        WriteToggle["💾 写入记忆"]
    end
    
    subgraph Status["状态指示"]
        Active["✅ 记忆激活"]
        Inactive["❌ 记忆关闭"]
    end
    
    Actions --> MemoryControls
    ReadToggle -->|开启| Active
    WriteToggle -->|关闭| Inactive
```

### 7.2 记忆管理页面

```mermaid
flowchart TB
    subgraph MemoryPage["记忆管理页面"]
        subgraph Header["头部"]
            Title["我的记忆"]
            Search["搜索框"]
            NewBtn["新建记忆"]
        end
        
        subgraph Filter["筛选栏"]
            CategoryFilter["分类：全部 | 用户 | 项目 | 反馈 | 参考"]
            Sort["排序：最近访问 | 创建时间 | 访问次数"]
        end
        
        subgraph List["记忆列表"]
            Item1["📝 用户偏好<br/><small>用户喜欢简洁回答，避免冗余</small>"]
            Item2["📁 当前项目<br/><small>正在开发用户认证模块，使用 JWT</small>"]
            Item3["💡 行为反馈<br/><small>用户不喜欢使用表情符号</small>"]
        end
        
        subgraph Detail["详情面板（点击展开）"]
            Content["完整 Markdown 内容"]
            EditBtn["编辑"]
            DeleteBtn["删除"]
        end
    end
    
    Header --> Filter
    Filter --> List
    List -->|点击| Detail
```

---

## 8. 接口设计

### 8.1 记忆管理接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/memories | 获取记忆列表（支持分类筛选） |
| POST | /api/memories | 创建记忆（用户手动创建） |
| GET | /api/memories/:id | 获取记忆详情 |
| PUT | /api/memories/:id | 更新记忆 |
| DELETE | /api/memories/:id | 删除记忆 |
| GET | /api/memories/index | 获取记忆索引（供 LLM 使用） |

### 8.2 聊天记忆接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/chat/memory/read | LLM 工具：读取记忆详情 |
| POST | /api/chat/memory/create | LLM 工具：创建记忆 |
| POST | /api/chat/memory/update | LLM 工具：更新记忆 |
| POST | /api/chat/memory/delete | LLM 工具：删除记忆 |
| POST | /api/chat/memory/list | LLM 工具：列出记忆 |

### 8.3 用户设置接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/settings/memory | 获取记忆设置 |
| PUT | /api/settings/memory | 更新记忆设置 |

---

## 9. 状态管理（可选）

### 9.1 状态分布建议

记忆相关的状态可以分散到现有的 Store 中，无需创建独立的 MemoryStore：

| 状态 | 建议存放位置 | 说明 |
|------|-------------|------|
| 记忆列表数据 | 页面本地 state / React Query | 通过 API 获取，无需全局状态 |
| 当前编辑的记忆 | 页面本地 state | 仅在管理页面使用 |
| 记忆读写开关 | `chat` Store 或 `user` Store | 跟随会话或用户偏好 |
| 加载状态 | 页面本地 state | 组件级管理 |

### 9.2 简单实现方式

```mermaid
flowchart TB
    subgraph LocalState["页面本地 State"]
        List["记忆列表"]
        Edit["编辑表单"]
        Filter["筛选条件"]
    end
    
    subgraph ChatStore["chat Store（已有）"]
        Toggle["记忆开关<br/>memoryReadEnabled<br/>memoryWriteEnabled"]
    end
    
    subgraph API["API 层"]
        TRPC["tRPC / REST"]
    end
    
    LocalState <-->|调用| API
    ChatStore -->|控制| LocalState
```

**理由**：
- 记忆数据主要是**列表展示**，用 React Query / SWR 管理即可
- 记忆管理页面是**独立页面**，状态无需共享给其他页面
- 只有记忆开关需要在**聊天界面**使用，放到 chat Store 足够

---

## 10. 配置项

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `memory.enabled` | boolean | true | 是否启用记忆系统 |
| `memory.maxEntriesPerUser` | number | 200 | 每用户最大记忆数 |
| `memory.defaultReadEnabled` | boolean | true | 默认开启记忆读取 |
| `memory.defaultWriteEnabled` | boolean | true | 默认开启记忆写入 |
| `memory.categories` | string[] | ['user', 'feedback', 'project', 'reference', 'general'] | 可用记忆分类 |

---

## 11. 向后扩展性设计

### 11.1 角色系统预留

```mermaid
flowchart TB
    subgraph Current["当前实现"]
        C1["agent_id = 'default_agent'"]
        C2["所有记忆归属同一角色"]
    end
    
    subgraph Migration["迁移路径"]
        M1["开发角色系统"] --> M2["创建角色表"]
        M2 --> M3["迁移 default_agent 到具体角色"]
        M3 --> M4["支持多角色记忆隔离"]
    end
    
    subgraph Future["未来功能"]
        F1["角色 A 记忆"]
        F2["角色 B 记忆"]
        F3["角色间记忆共享"]
    end
    
    Current --> Migration
    Migration --> Future
```

### 11.2 预留字段说明

| 字段 | 当前值 | 未来用途 |
|------|--------|----------|
| `agent_id` | 'default_agent' | 关联到具体角色 ID |
| `source_session_id` | 可选 | 追踪记忆来源会话 |
| `source_message_ids` | JSON 数组 | 精确定位来源消息 |

---

## 12. 与现有系统共存

### 12.1 与向量记忆系统的关系

```mermaid
flowchart TB
    subgraph Existing["现有系统"]
        VectorMemory["向量记忆<br/>user_memories"]
    end
    
    subgraph New["新系统"]
        TextMemory["文本记忆<br/>text_memories"]
    end
    
    subgraph Coexist["共存策略"]
        Independent["独立运行"]
        UserChoice["用户选择使用哪种"]
        FutureMerge["未来评估合并"]
    end
    
    VectorMemory --> Independent
    TextMemory --> Independent
    Independent --> UserChoice
    UserChoice --> FutureMerge
```

**共存原则**：
- 新系统使用独立的表（`text_memories`, `text_memory_index`）
- 不与现有的 `user_memories` 表交互
- 用户可在设置中选择使用哪种记忆系统
- 未来根据使用反馈评估是否迁移/合并

---

## 13. 总结

本记忆设计方案采用 Claude Code 的文本索引架构，核心特点：

1. **两表结构**：索引表 + 条目表，简单清晰
2. **LLM 驱动**：由 LLM 自主判断记忆相关性，无需向量计算
3. **完全可读**：Markdown 格式，用户可直接查看编辑
4. **工具调用**：通过函数调用实现记忆的 CRUD 操作
5. **预留扩展**：`agent_id` 固定为 `default_agent`，未来无缝支持多角色

关键优势：
- 消除 Embedding 模型依赖
- 提升用户可控性和可观测性
- 简化数据库结构
- 降低系统复杂度
