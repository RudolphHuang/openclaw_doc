# AINFT API 多格式接入技术方案

## 1. 需求概述

### 1.1 背景
当前AINFT API已支持 `/v1/chat/completions` (OpenAI格式) 和 `/v1/messages` (Claude格式) 的模型调用，但在未付费用户访问控制、模型元数据准确性等方面需要完善。

### 1.2 新需求

| 序号 | 需求 | 优先级 |
|------|------|--------|
| 1 | 未付费用户调用Premium模型时给出合适的报错信息 | 高 |
| 2 | `supported_endpoint_types` 字段根据测试结果调整 | 中 |
| 3 | `models`接口中 `owned_by` 字段改成正确的provider | 中 |

---

## 2. 系统架构

### 2.1 整体架构图

```mermaid
graph TB
    subgraph Client["客户端"]
        C1["OpenAI SDK"]
        C2["Claude SDK"]
        C3["其他HTTP客户端"]
    end
    
    subgraph APILayer["API接入层"]
        R1["/v1/chat/completions"]
        R2["/v1/messages"]
        R3["/v1/models"]
    end
    
    subgraph AuthLayer["认证层"]
        A1["ExternalAuth<br/>外部鉴权"]
        A2["TokenAuth<br/>令牌鉴权"]
        A3["TokenOrExternalAuth<br/>混合鉴权"]
    end
    
    subgraph MiddlewareLayer["中间件层"]
        M1["HttpNodeRestrict<br/>节点限制"]
        M2["SystemPerformanceCheck<br/>性能检查"]
        M3["ApiKeyRechargeRateLimit<br/>限流"]
        M4["Distribute<br/>渠道分发"]
    end
    
    subgraph RelayLayer["转发层"]
        RL1["Relay Controller"]
        RL2["Format Converter<br/>格式转换器"]
    end
    
    subgraph ChannelLayer["渠道层"]
        CH1["OpenAI Channel"]
        CH2["Claude Channel"]
        CH3["SiliconFlow Channel"]
        CH4["...其他渠道"]
    end
    
    C1 --> R1
    C2 --> R2
    C1 --> R3
    C2 --> R3
    
    R1 --> A1
    R2 --> A1
    R3 --> A3
    
    A1 --> M1
    A2 --> M1
    M1 --> M2
    M2 --> M3
    M3 --> M4
    M4 --> RL1
    
    RL1 --> RL2
    RL2 --> CH1
    RL2 --> CH2
    RL2 --> CH3
    RL2 --> CH4
```

### 2.2 请求处理流程

```mermaid
sequenceDiagram
    participant Client as 客户端
    participant Router as Router
    participant Auth as 认证中间件
    participant Distribute as 分发中间件
    participant Relay as Relay控制器
    participant Channel as 渠道适配器
    participant Upstream as 上游服务

    Client->>Router: POST /v1/messages (Claude格式)
    
    Note over Router: extAuthChatRouter<br/>外部鉴权路由组
    
    Router->>Auth: ExternalAuth()
    Auth->>Auth: 从Authorization或<br/>x-api-key解析API Key
    Auth->>Auth: 调用外部鉴权服务<br/>POST /v1/internal/auth/verify
    Auth-->>Router: 设置Context<br/>user_id=0 (外部用户)
    
    Router->>Distribute: Distribute()
    Distribute->>Distribute: 检查token_model_limit
    Distribute->>Distribute: 选择渠道
    Distribute-->>Router: 设置Channel信息
    
    Router->>Relay: Relay(c, RelayFormatClaude)
    Relay->>Relay: 解析Claude请求
    Relay->>Channel: 获取渠道适配器
    
    alt Claude渠道
        Channel->>Channel: 直接转发Claude格式
    else 其他渠道
        Channel->>Channel: Claude格式→OpenAI格式转换
    end
    
    Channel->>Upstream: 转发请求
    Upstream-->>Channel: 返回响应
    Channel-->>Relay: 返回标准格式
    Relay-->>Client: 返回Claude格式响应
```

---

## 3. 需求详细设计

### 3.1 需求1：未付费用户Premium模型访问控制

#### 3.1.1 业务流程

```mermaid
flowchart TD
    A[用户请求<br/>/v1/chat/completions<br/>或/v1/messages] --> B{外部鉴权}
    B -->|成功| C[设置Context<br/>id=0, external_user_id=xxx]
    B -->|失败| Z[返回401未授权]
    
    C --> D[Distribute中间件]
    D --> E{检查模型限制}
    
    E -->|token_model_limit_enabled=false| F[允许访问所有模型]
    E -->|token_model_limit_enabled=true| G{模型在允许列表?}
    
    G -->|是| H[允许访问]
    G -->|否| I{Premium模型?}
    
    I -->|是| J[返回403错误<br/>Premium模型需要付费订阅]
    I -->|否| K[返回403错误<br/>无权访问该模型]
    
    F --> L[选择渠道]
    H --> L
    L --> M[转发请求]
```

#### 3.1.2 关键逻辑

| 检查点 | 位置 | 处理方式 |
|--------|------|----------|
| 外部用户识别 | `middleware/external_auth.go` | `id=0` 表示外部鉴权用户 |
| 模型限制检查 | `middleware/distributor.go` | 检查 `token_model_limit` |
| Premium模型判断 | 需新增配置 | 根据模型标签或供应商判断 |
| 错误返回 | `middleware/distributor.go` | 返回OpenAI格式错误响应 |

#### 3.1.3 错误响应格式

```json
{
  "error": {
    "message": "Premium模型需要付费订阅，请联系管理员升级账户",
    "type": "insufficient_quota",
    "param": "model",
    "code": "premium_model_forbidden"
  }
}
```

---

### 3.2 需求2：supported_endpoint_types字段调整

#### 3.2.1 当前端点类型定义

```mermaid
classDiagram
    class EndpointType {
        <<enumeration>>
        openai
        openai-response
        openai-response-compact
        anthropic
        gemini
        jina-rerank
        image-generation
        embeddings
        openai-video
    }
    
    class ChannelType {
        <<enumeration>>
        ChannelTypeAnthropic
        ChannelTypeGemini
        ChannelTypeSiliconFlow
        ChannelTypeOpenAI
        ...
    }
    
    class Model {
        +string ModelName
        +EndpointType[] SupportedEndpointTypes
        +string OwnedBy
    }
    
    ChannelType --> EndpointType : 映射
    Model --> EndpointType : 支持
```

#### 3.2.2 端点类型映射关系

| 渠道类型 | 支持的EndpointTypes | 优先级 |
|----------|---------------------|--------|
| Anthropic | anthropic, openai | anthropic优先 |
| Gemini | gemini, openai | gemini优先 |
| SiliconFlow | openai | - |
| OpenAI | openai | - |
| xAI | openai, openai-response | openai优先 |
| Sora | openai-video | - |
| Jina | jina-rerank | - |
| 图片生成模型 | image-generation, openai | image-generation优先 |

#### 3.2.3 调整策略

```mermaid
flowchart LR
    A[模型请求] --> B{渠道类型}
    
    B -->|Anthropic| C[返回anthropic, openai]
    B -->|Gemini| D[返回gemini, openai]
    B -->|SiliconFlow| E[返回openai]
    B -->|OpenAI| F[返回openai]
    B -->|xAI| G[返回openai, openai-response]
    
    C --> H[Models接口]
    D --> H
    E --> H
    F --> H
    G --> H
```

---

### 3.3 需求3：owned_by字段修正

#### 3.3.1 当前问题

当前`owned_by`字段来源于`adaptor.GetChannelName()`，返回的是渠道名称（如"claude"、"siliconflow"），而非实际的模型供应商。

#### 3.3.2 供应商映射规则

根据`llm.csv`中的定义，模型与供应商的映射关系如下：

| 模型ID模式 | 供应商(owned_by) |
|------------|------------------|
| gpt-* | OpenAI |
| claude-* | Anthropic |
| gemini-* | Google |
| minimax-* | MiniMax |
| kimi-* | Moonshot |
| glm-* | 智谱/Z.ai |
| deepseek-* | DeepSeek |

#### 3.3.3 修正方案

```mermaid
sequenceDiagram
    participant Client as 客户端
    participant Controller as Model Controller
    participant Pricing as Pricing Service
    participant Vendor as Vendor Service
    
    Client->>Controller: GET /v1/models
    Controller->>Pricing: GetPricing()
    Pricing->>Pricing: 构建模型列表
    
    loop 每个模型
        Pricing->>Vendor: 根据模型名匹配供应商
        Vendor->>Vendor: 匹配defaultVendorRules
        Vendor-->>Pricing: 返回VendorID和VendorName
        Pricing->>Pricing: 设置OwnerBy=VendorName
    end
    
    Pricing-->>Controller: 返回模型列表
    Controller-->>Client: 返回带正确owned_by的响应
```

#### 3.3.4 供应商规则配置

```yaml
# 供应商匹配规则 (model/pricing_default.go)
vendor_rules:
  gpt: OpenAI
  dall-e: OpenAI
  whisper: OpenAI
  o1: OpenAI
  o3: OpenAI
  claude: Anthropic
  gemini: Google
  moonshot: Moonshot
  kimi: Moonshot
  chatglm: 智谱
  glm-: 智谱
  qwen: 阿里巴巴
  deepseek: DeepSeek
  abab: MiniMax
  ernie: 百度
  spark: 讯飞
  hunyuan: 腾讯
  command: Cohere
  mistral: Mistral
  grok: xAI
  llama: Meta
```

---

## 4. 关键代码位置

### 4.1 路由配置

```mermaid
graph LR
    A[router/relay-router.go] --> B[extAuthChatRouter]
    A --> C[modelsRouter]
    
    B --> D[POST /v1/chat/completions]
    B --> E[POST /v1/messages]
    
    C --> F[GET /v1/models]
    C --> G[GET /v1/models/:model]
```

### 4.2 核心文件

| 功能 | 文件路径 |
|------|----------|
| 外部鉴权 | `middleware/external_auth.go` |
| 渠道分发 | `middleware/distributor.go` |
| 节点限制 | `middleware/http_node_restrict.go` |
| 模型列表 | `controller/model.go` |
| 定价/供应商 | `model/pricing.go` |
| 供应商映射 | `model/pricing_default.go` |
| 端点类型 | `common/endpoint_type.go` |
| Claude适配器 | `relay/channel/claude/adaptor.go` |
| OpenAI适配器 | `relay/channel/openai/adaptor.go` |

---

## 5. 接口变更

### 5.1 /v1/models 响应格式

```json
{
  "success": true,
  "data": [
    {
      "id": "claude-opus-4-6",
      "object": "model",
      "created": 1626777600,
      "owned_by": "Anthropic",
      "supported_endpoint_types": ["anthropic", "openai"]
    },
    {
      "id": "chatgpt-5-2",
      "object": "model",
      "created": 1626777600,
      "owned_by": "OpenAI",
      "supported_endpoint_types": ["openai"]
    },
    {
      "id": "minimax-m2-5",
      "object": "model",
      "created": 1626777600,
      "owned_by": "MiniMax",
      "supported_endpoint_types": ["openai"]
    }
  ],
  "object": "list"
}
```

### 5.2 错误响应格式

```json
{
  "error": {
    "message": "Premium模型需要付费订阅，请联系管理员升级账户",
    "type": "insufficient_quota",
    "param": "model",
    "code": "premium_model_forbidden"
  }
}
```

---

## 6. 测试要点

### 6.1 功能测试

| 测试项 | 测试场景 | 预期结果 |
|--------|----------|----------|
| Claude接口 | 使用x-api-key访问/v1/messages | 正常返回 |
| OpenAI接口 | 使用Authorization访问/v1/chat/completions | 正常返回 |
| 未付费用户 | 访问Premium模型 | 返回403错误 |
| 已付费用户 | 访问Premium模型 | 正常返回 |
| 模型列表 | 检查owned_by字段 | 与llm.csv一致 |
| 模型列表 | 检查supported_endpoint_types | 与渠道类型匹配 |

### 6.2 兼容性测试

- Claude SDK兼容性
- OpenAI SDK兼容性
- 第三方工具兼容性 (Cursor, Continue等)

---

## 7. 部署注意事项

1. **HTTP节点限制**: 确保`/v1/messages`已加入`HttpNodeAllowedPaths`
2. **外部鉴权**: 确保`EXTERNAL_AUTH_SERVICE_URL`环境变量已配置
3. **供应商数据**: 首次部署时自动创建供应商记录
4. **缓存刷新**: 模型元数据变更后需刷新定价缓存
